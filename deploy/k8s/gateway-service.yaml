apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: capyclaw
  labels:
    app.kubernetes.io/name: gateway
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 18789
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: gateway
