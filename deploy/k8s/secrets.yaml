# External secrets template — replace with actual values or use a SecretStore
# In production, use ExternalSecrets Operator or Vault CSI Provider
apiVersion: v1
kind: Secret
metadata:
  name: capyclaw-secrets
  namespace: capyclaw
type: Opaque
stringData:
  postgres-password: "REPLACE_ME"
  anthropic-api-key: "REPLACE_ME"
  openai-api-key: "REPLACE_ME"
  jwt-signing-key: "REPLACE_ME"
